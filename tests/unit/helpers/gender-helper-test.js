import { genderHelper } from 'legislator-lookup/helpers/gender-helper';
import { module, test } from 'qunit';

module('Unit | Helper | gender helper');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = genderHelper([42]);
  assert.ok(result);
});
